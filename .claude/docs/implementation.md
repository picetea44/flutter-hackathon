# 테스트의 이름

- 테스트의 이름은 `method명_fail` 혹은 `method명_success` 를 기본으로 한다.
- `fail`의 경우에는 추가적으로 `causeOf` 를 붙여 오류 원인을 명확히 메서드 명에 포함한다.
- `@DisplayName`을 사용하여 명확하게 한국어로 테스트의 목적에 대해 작성한다

# API 문서

- API는 `README.md` 에 추가한다.
